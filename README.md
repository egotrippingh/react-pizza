# React Pizza Site 🍕

Современное SPA-приложение каталога пиццы на React с полным функционалом интернет-магазина. Включает каталог товаров с фильтрацией, поиском, сортировкой, пагинацией, корзину покупок и адаптивный дизайн.

## 🚀 Основные возможности

- **Каталог пицц** с загрузочными скелетонами
- **Фильтрация по категориям** (Все, Мясные, Вегетарианские, Гриль, Острые, Закрытые)
- **Сортировка** по популярности, цене и алфавиту (по возрастанию/убыванию)
- **Поиск** по названию пиццы
- **Пагинация** для удобной навигации по товарам
- **Корзина покупок** с возможностью оформления заказа
- **Адаптивный дизайн** для всех устройств
- **Роутинг** между страницами

## 🛠 Технологический стек

- **React 19** + **Vite** (роллап-билд `rolldown-vite@7.1.12`)
- **React Router v7** (`react-router`, `react-router-dom`)
- **Sass/SCSS** (через `sass-embedded`), модульные SCSS для компонентов
- **ESLint 9** с настройками для React и TypeScript
- **React Compiler** (плагин `babel-plugin-react-compiler` включён)
- **react-content-loader** для скелетонов загрузки
- **react-paginate** для пагинации

## 📦 Установка и запуск

### Предварительные требования

- Node.js 18+ (рекомендуется актуальная LTS)
- npm 9+

### Быстрый старт

```bash
# Клонирование репозитория
git clone <repo-url>
cd reactpizzasite

# Установка зависимостей
npm ci            # или npm install

# Запуск в режиме разработки
npm run start
```

Откройте `http://localhost:5173` в браузере (порт может отличаться в вашей среде).

### Доступные скрипты

```bash
# Запуск дев-сервера с hot reload
npm run start

# Продакшн-сборка проекта
npm run build

# Предпросмотр продакшн-сборки
npm run preview

# Линтинг кода
npm run lint
```

## 📁 Структура проекта

```text
reactpizzasite/
├── public/                    # Статические файлы
│   ├── close.png             # Иконка закрытия
│   ├── pizzaImage.jpg        # Изображение пиццы
│   └── search.png            # Иконка поиска
├── src/
│   ├── assets/               # Ресурсы приложения
│   │   ├── pizza_logo.png    # Логотип пиццерии
│   │   └── shopping-cart.png # Иконка корзины
│   ├── components/           # Переиспользуемые компоненты
│   │   ├── Header/           # Шапка сайта (логотип + поиск + корзина)
│   │   │   ├── index.jsx
│   │   │   └── Header.scss
│   │   ├── Logo/             # Компонент логотипа
│   │   ├── Search/           # Компонент поиска
│   │   │   ├── index.jsx
│   │   │   └── Search.module.scss
│   │   ├── CartIcon/         # Иконка корзины
│   │   ├── Categories/       # Фильтр по категориям
│   │   │   ├── index.jsx
│   │   │   └── Categories.scss
│   │   ├── Sort/             # Компонент сортировки
│   │   │   ├── index.jsx
│   │   │   └── Sort.scss
│   │   ├── Pizza/            # Карточка пиццы
│   │   │   ├── index.jsx
│   │   │   └── Pizza.scss
│   │   ├── PizzaSkeleton/    # Скелетон загрузки карточки
│   │   ├── Pagination/       # Компонент пагинации
│   │   │   ├── index.jsx
│   │   │   └── Pagination.module.scss
│   │   └── NotFoundBlock/    # Компонент 404 ошибки
│   ├── pages/                # Страницы приложения
│   │   ├── Home.jsx          # Главная страница (каталог)
│   │   ├── Cart.jsx          # Страница корзины
│   │   └── NotFound.jsx      # Страница 404
│   ├── App.jsx               # Корневой компонент с роутингом
│   ├── main.jsx              # Точка входа приложения
│   ├── App.css               # Глобальные стили
│   └── index.css             # Базовые стили
├── vite.config.ts            # Конфигурация Vite
├── eslint.config.js          # Конфигурация ESLint
├── tsconfig.*.json           # Конфигурация TypeScript
└── package.json              # Зависимости и скрипты
```

## 🧭 Навигация и маршрутизация

Приложение использует **React Router v7** для клиентской маршрутизации:

- **`/`** - Главная страница с каталогом пицц
- **`/cart`** - Страница корзины покупок
- **`/*`** - Страница 404 для несуществующих маршрутов

### Основные маршруты

```jsx
<Routes>
  <Route path="/" element={<Home searchValue={searchValue} />} />
  <Route path="/cart" element={<Cart />} />
  <Route path="*" element={<NotFound />} />
</Routes>
```

Входная точка `src/main.jsx` оборачивает приложение в `BrowserRouter`, обеспечивая SPA-навигацию без перезагрузки страницы.

## 🔌 API и загрузка данных

Приложение использует **Mock API** для получения данных о пиццах:

### Базовый URL API

```
https://68e2aa938e14f4523dab802e.mockapi.io/items
```

### Параметры запроса

| Параметр   | Описание                         | Пример             |
| ---------- | -------------------------------- | ------------------ |
| `page`     | Номер страницы для пагинации     | `page=1`           |
| `limit`    | Количество элементов на странице | `limit=6`          |
| `category` | ID категории (0 = все)           | `category=1`       |
| `sortBy`   | Поле для сортировки              | `sortBy=rating`    |
| `order`    | Порядок сортировки               | `order=desc`       |
| `search`   | Поисковый запрос                 | `search=пепперони` |

### Пример запроса

```javascript
const url = `https://68e2aa938e14f4523dab802e.mockapi.io/items?page=${currentPage}&limit=6&${
  categoryId > 0 ? `category=${categoryId}&` : ""
}sortBy=${sortType.sortProp}&order=${descSort ? "desc" : "asc"}${search}`;

const response = await fetch(url);
const data = await response.json();
```

### Состояния загрузки

- **Загрузка**: Отображаются скелетоны `PizzaSkeleton`
- **Успех**: Рендерятся карточки пицц `Pizza`
- **Ошибка**: Выводится сообщение об ошибке в консоль

## 🎨 Стилизация и дизайн

Проект использует **SCSS** (`sass-embedded`) для стилизации с модульным подходом:

### Структура стилей

- **Глобальные стили**: `App.css`, `index.css`
- **Компонентные стили**:
  - `Header.scss` - стили шапки
  - `Categories.scss` - стили фильтра категорий
  - `Sort.scss` - стили сортировки
  - `Pizza.scss` - стили карточки пиццы
- **Модульные стили**:
  - `Search.module.scss` - стили поиска
  - `Pagination.module.scss` - стили пагинации

### Особенности стилизации

- **Адаптивный дизайн** для мобильных устройств
- **CSS Modules** для изоляции стилей компонентов
- **SCSS переменные** для единообразия цветов и размеров
- **Flexbox/Grid** для современной верстки

## ⚙️ Конфигурация

### Vite конфигурация

```typescript
// vite.config.ts
export default defineConfig({
  plugins: [
    react({
      babel: {
        plugins: [["babel-plugin-react-compiler"]],
      },
    }),
  ],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
    },
  },
});
```

### TypeScript поддержка

Хотя исходный код написан на JS/JSX, проект настроен для TypeScript:

- `tsconfig.app.json` - конфигурация для приложения
- `tsconfig.node.json` - конфигурация для Node.js
- `tsconfig.json` - базовая конфигурация

Это упрощает постепенный переход на TypeScript при необходимости.

### ESLint правила

Строгие правила линтинга для обеспечения качества кода:

- React Hooks правила
- React Refresh плагин
- TypeScript поддержка

## 📋 Компоненты и их функциональность

### Основные компоненты

#### `Header` - Шапка сайта

- **Логотип** пиццерии
- **Поиск** по названию пицц
- **Иконка корзины** с переходом на страницу корзины

#### `Categories` - Фильтр категорий

- **Все** (ID: 0)
- **Мясные** (ID: 1)
- **Вегетарианские** (ID: 2)
- **Гриль** (ID: 3)
- **Острые** (ID: 4)
- **Закрытые** (ID: 5)

#### `Sort` - Сортировка

- **По популярности** (`rating`)
- **По цене** (`price`)
- **По алфавиту** (`title`)
- **Направление**: по возрастанию ↑ / убыванию ↓

#### `Pizza` - Карточка пиццы

- Изображение пиццы
- Название и описание
- Цена и рейтинг
- Кнопка добавления в корзину

#### `Pagination` - Пагинация

- Навигация по страницам
- Отображение текущей страницы
- Переход к предыдущей/следующей странице

#### `Cart` - Корзина покупок

- Список добавленных товаров
- Количество и цена каждого товара
- Итоговая сумма заказа
- Кнопки "Продолжить покупки" и "Оформить заказ"

## 🚀 Развертывание

### Продакшн сборка

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`.

### Предпросмотр продакшн версии

```bash
npm run preview
```

## 📝 Требования к системе

- **Node.js** 18+ (рекомендуется актуальная LTS)
- **npm** 9+
- **Современный браузер** с поддержкой ES6+

## 🔗 Полезные пути

- **Точка входа**: `src/main.jsx`
- **Корневой компонент**: `src/App.jsx`
- **Главная страница**: `src/pages/Home.jsx`
- **Настройки Vite**: `vite.config.ts`
- **ESLint конфиг**: `eslint.config.js`

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 👨‍💻 Автор - egotrippingh

Создано с ❤️ для демонстрации современных возможностей React и экосистемы веб-разработки.

---

**Примечание**: Это демонстрационный проект для изучения React, Vite и современных инструментов веб-разработки.
